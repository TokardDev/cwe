# CWE-1234 - Hardware Internal or Debug Modes Allow Override of Locks



## ⚠️ POTENTIAL RELATED CVEs (NOT FROM MITRE — BASED ON SIMILARITY ANALYSIS)

> **Note:** The following CVEs are **not officially listed** on the MITRE CWE page for this weakness.
> They are included based on technical similarity and may be relevant for threat assessment.

| CVE | Description |
|-----|-------------|
| [CVE-2019-0090](https://www.cve.org/CVERecord?id=CVE-2019-0090) | Intel CSME ROM vulnerability allows debug mode to override security locks |

## ⚠️ POTENTIAL RELATED CVEs (NOT FROM MITRE — BASED ON SIMILARITY ANALYSIS)

> **Note:** The following CVEs are **not officially listed** on the MITRE CWE page for this weakness.
> They are included based on technical similarity and may be relevant for threat assessment.

| CVE | Description |
|-----|-------------|

## CWE Explanation

This weakness occurs when hardware internal or debug modes allow bypassing or overriding security lock bits. Lock bits are commonly used to protect sensitive system configurations (e.g., memory protection unit settings, cryptographic keys) after they have been programmed by trusted firmware during the boot process.

While these locks are intended to prevent any further modification, hardware designs sometimes include "backdoors" or override signals for debugging or manufacturing test purposes (e.g., scan mode, debug mode). If these override mechanisms are not properly restricted or authenticated, an attacker who can trigger these modes can disable the locks and modify protected system settings.

The root cause is a design flaw where debug or test features are given higher priority than security protection mechanisms, without adequate access control for those powerful features.

## Impact Assessment

| Aspect | Impact |
|--------|--------|
| **Confidentiality** | Low impact (*System configuration exposure is possible*) |
| **Integrity** | High - Attacker can modify locked system configurations and security settings. |
| **Availability** | Low impact (*No direct impact on availability*) |

## Attack Scenarios

### Scenario 1
An attacker is able to trigger the hardware "scan mode" on a device, which is typically used for factory testing. In the next step, the attacker uses this mode to override established lock bits that protect memory protection unit (MPU) configurations. By bypassing these locks through the test interface, the attacker successfully modifies the MPU settings to gain unauthorized access to restricted memory regions containing sensitive data.

### Scenario 2
An attacker triggers the system debug mode through a JTAG interface. In the next step, the attacker observes that the hardware design incorrectly resets all security-critical register locks when the `jtag_unlock` signal is asserted. This allows the attacker, now in debug mode, to modify sensitive configuration registers that were previously locked by secure boot firmware, effectively compromising the system's security foundation.

---

## MITRE Reference Data

### Description

System configuration protection may be bypassed during debug mode.

### Extended Description

Device configuration controls are commonly programmed after a device power reset by a trusted firmware or software module (e.g., BIOS/bootloader) and then locked from any further modification. This is commonly implemented using a trusted lock bit, which when set, disables writes to a protected set of registers or address regions. The lock protection is intended to prevent modification of certain system configuration (e.g., memory/memory protection unit configuration). If debug features supported by hardware or internal modes/system states are supported in the hardware design, modification of the lock protection may be allowed allowing access and modification of configuration information.

### Demonstrative Examples

#### Example 1

For example, consider the example Locked_override_register example. This register module supports a lock mode that blocks any writes after lock is set to 1.

However, it also allows override of the lock protection when scan_mode or debug_unlocked modes are active.

```verilog
module Locked_register_example
(
input [15:0] Data_in,
input Clk,
input resetn,
input write,
input Lock,
input scan_mode,
input debug_unlocked,
output reg [15:0] Data_out
);

reg lock_status;

always @(posedge Clk or negedge resetn)
begin
	if (~resetn) // Register is reset resetn
	begin
	  lock_status <= 1'b0;
	end
	else if (Lock)
	begin
	  lock_status <= 1'b1;
	end
	else if (~Lock)
	begin
	  lock_status <= lock_status
	end
end
always @(posedge Clk or negedge resetn)
begin
	if (~resetn) // Register is reset resetn
	begin
	  Data_out <= 16'h0000;
	end
	else if (write & (~lock_status | scan_mode | debug_unlocked) ) // Register protected by Lock bit input, overrides supported for scan_mode & debug_unlocked
	begin
	  Data_out <= Data_in;
	end
	else if (~write)
	begin
	  Data_out <= Data_out;
	end
end
endmodule
```

If either the scan_mode or the debug_unlocked modes can be triggered by software, then the lock protection may be bypassed.

Either remove the debug and scan mode overrides or protect enabling of these modes so that only trusted and authorized users may enable these modes.

#### Example 2

The following example code [REF-1375] is taken from the register lock security peripheral of the HACK@DAC'21 buggy OpenPiton SoC. It demonstrates how to lock read or write access to security-critical hardware registers (e.g., crypto keys, system integrity code, etc.). The configuration to lock all the sensitive registers in the SoC is managed through the reglk_mem registers. These reglk_mem registers are reset when the hardware powers up and configured during boot up. Malicious users, even with kernel-level software privilege, do not get access to the sensitive contents that are locked down. Hence, the security of the entire system can potentially be compromised if the register lock configurations are corrupted or if the register locks are disabled.

```verilog
...
always @(posedge clk_i)
begin
	if(~(rst_ni && ~jtag_unlock && ~rst_9))
	begin
		for (j=0; j < 6; j=j+1) begin
			reglk_mem[j] <= 'h0;
		end
	end
end
...
```

The example code [REF-1375] illustrates an instance of a vulnerable implementation of register locks in the SoC. In this flawed implementation [REF-1375], the reglk_mem registers are also being reset when the system enters debug mode (indicated by the jtag_unlock signal). Consequently, users can simply put the processor in debug mode to access sensitive contents that are supposed to be protected by the register lock feature.

This can be mitigated by excluding debug mode signals from the reset logic of security-critical register locks as demonstrated in the following code snippet [REF-1376].

```verilog
...
always @(posedge clk_i)
begin
	if(~(rst_ni && ~rst_9))
	begin
		for (j=0; j < 6; j=j+1) begin
			reglk_mem[j] <= 'h0;
		end
	end
end
...
```

## Remediations from MITRE

| Phase | Strategy | Mitigation | Effectiveness |
|-------|----------|------------|---------------|
| Architecture and Design; Implementation; Testing | - | Security Lock bit protections should be reviewed for any bypass/override modes supported. Any supported override modes either should be removed or protected using authenticated debug modes. Security lock programming flow and lock properties should be tested in pre-silicon and post-silicon testing. | High |

*Source: MITRE CWE Database*

## Recommended Remediations

| Phase | Remediation | Priority |
|-------|-------------|----------|
| Architecture and Design | Remove all debug/test override paths for security lock bits in production silicon. If debug override is required, gate it behind authenticated debug (e.g., challenge-response over JTAG). | Critical |
| Implementation | Ensure that scan mode, debug unlock, and any internal test signals do not appear in the reset or write-enable logic of security-critical lock registers. | Critical |
| Verification | Use formal verification to prove that no combination of debug, scan, or test mode signals can clear or bypass a set lock bit. | High |
| Testing | In post-silicon, activate every debug and test mode and verify that all security lock bits remain enforced and protected registers remain immutable. | High |

*These remediations are recommended best practices and are not sourced from MITRE.*
