# CWE-1243 - Sensitive Non-Volatile Information Not Protected During Debug


## ⚠️ POTENTIAL RELATED CVEs (NOT FROM MITRE — BASED ON SIMILARITY ANALYSIS)

> **Note:** The following CVEs are **not officially listed** on the MITRE CWE page for this weakness.
> They are included based on technical similarity and may be relevant for threat assessment.

| CVE | Description |
|-----|-------------|
| [CVE-2019-0090](https://www.cve.org/CVERecord?id=CVE-2019-0090) | Intel CSME debug vulnerability exposes hardware key and non-volatile secrets |

## CWE Explanation

This weakness occurs when sensitive information stored in hardware fuses (such as root keys, encryption keys, or manufacturing data) is not adequately protected from access when the system is in debug mode. Fuses are often used to store permanent, security-critical values that are sensed during boot and stored in microarchitectural registers for runtime use.

In a secure design, these sensitive values should be disconnected from the debug interface or masked when a debugger is attached. If this protection is missing, an attacker capable of initiating a debug session can read these values directly from the fuses or their reflected register locations, compromising the system's root of trust or other cryptographic protections.

The root cause is a failure to implement logic that automatically blocks access to sensitive non-volatile storage when the chip transitions to a debug or test state.

## Impact Assessment

| Aspect | Impact |
|--------|--------|
| **Confidentiality** | High - Attacker can extract permanent root keys and other sensitive fuse data. |
| **Integrity** | Low impact (*Attacker can modify reflected registers in memory, but not the fuses themselves*) |
| **Availability** | Low impact (*No direct impact on availability*) |

## Attack Scenarios

### Scenario 1
An attacker initiates a debug session on a System-on-Chip (SoC) using a physical probe. In the next step, the attacker reads various microarchitectural registers that store sensitive manufacturing information, such as die serial numbers and wafer coordinates, which were sensed from fuses at boot time. Because the hardware design fails to disconnect these registers from the debug interface, the attacker successfully extracts this data to facilitate further targeted attacks or counterfeit detection bypass.

### Scenario 2
An attacker targets an SoC that uses an integrated AES accelerator with keys stored in non-volatile fuses. In the next step, the attacker puts the system into debug mode and observes that while some key registers are correctly zeroed out by the hardware, one specific key register remains connected to its internal signal path. By accessing this unmasked register through the debug interface, the attacker successfully extracts a sensitive encryption key used by the operating system, compromising the confidentiality of all data protected by that key.

---

## MITRE Reference Data

### Description

Access to security-sensitive information stored in fuses is not limited during debug.

### Extended Description

Several security-sensitive values are programmed into fuses to be used during early-boot flows or later at runtime. Examples of these security-sensitive values include root keys, encryption keys, manufacturing-specific information, chip-manufacturer-specific information, and original-equipment-manufacturer (OEM) data. After the chip is powered on, these values are sensed from fuses and stored in temporary locations such as registers and local memories. These locations are typically access-control protected from untrusted agents capable of accessing them. Even to trusted agents, only read-access is provided.

### Demonstrative Examples

#### Example 1

Sensitive manufacturing data (such as die information) are stored in fuses. When the chip powers on, these values are read from the fuses and stored in microarchitectural registers. These registers are only given read access to trusted software running on the core. Untrusted software running on the core is not allowed to access these registers.

All microarchitectural registers in this chip can be accessed through the debug interface. As a result, even an untrusted debugger can access this data and retrieve sensitive manufacturing data.

Registers used to store sensitive values read from fuses should be blocked during debug. These registers should be disconnected from the debug interface.

#### Example 2

The example code below is taken from one of the AES cryptographic accelerators of the HACK@DAC'21 buggy OpenPiton SoC [REF-1366]. The operating system (OS) uses three AES keys to encrypt and decrypt sensitive data using this accelerator. These keys are sensitive data stored in fuses. The security of the OS will be compromised if any of these AES keys are leaked. During system bootup, these AES keys are sensed from fuses and stored in temporary hardware registers of the AES peripheral. Access to these temporary registers is disconnected during the debug state to prevent them from leaking through debug access. In this example (see the vulnerable code source), the registers key0, key1, and key2 are used to store the three AES keys (which are accessed through key_big0, key_big1, and key_big2 signals). The OS selects one of these three keys through the key_big signal, which is used by the AES engine.

```verilog
...
assign key_big0    = debug_mode_i ? 192'b0 : {key0[0],
key0[1], key0[2], key0[3], key0[4], key0[5]};

assign key_big1    = debug_mode_i ? 192'b0 : {key1[0],
key1[1], key1[2], key1[3], key1[4], key1[5]};

assign key_big2    = {key2[0], key2[1], key2[2],
key2[3], key2[4], key2[5]};
...
assign key_big = key_sel[1] ? key_big2 : ( key_sel[0] ?
key_big1 : key_big0 );
...
```

The above code illustrates an instance of a vulnerable implementation for blocking AES key mechanism when the system is in debug mode (i.e., when debug_mode_i is asserted). During debug mode, key accesses through key_big0 and key_big1 are effectively disconnected, as their values are set to zero. However, the key accessed via the key_big2 signal remains accessible, creating a potential pathway for sensitive fuse data leakage, specifically AES key2, during debug mode. Furthermore, even though it is not strictly necessary to disconnect the key_big signal when entering debug mode (since disconnecting key_big0, key_big1, and key_big2 will inherently disconnect key_big), it is advisable, in line with the defense-in-depth strategy, to also sever the connection to key_big. This additional security measure adds an extra layer of protection and safeguards the AES keys against potential future modifications to the key_big logic.

To mitigate this, disconnect access through key_big2 and key_big during debug mode [REF-1367].

```verilog
...
assign key_big0    = debug_mode_i ? 192'b0 : {key0[0],
key0[1], key0[2], key0[3], key0[4], key0[5]};

assign key_big1    = debug_mode_i ? 192'b0 : {key1[0],
key1[1], key1[2], key1[3], key1[4], key1[5]}; 

assign key_big2    = debug_mode_i ? 192'b0 : {key2[0],
key2[1], key2[2], key2[3], key2[4], key2[5]};
...
assign key_big = debug_mode_i ? 192'b0 : (  key_sel[1] ?
key_big2 : ( key_sel[0] ? key_big1 : key_big0 )  );
...
```

## Remediations from MITRE

| Phase | Strategy | Mitigation | Effectiveness |
|-------|----------|------------|---------------|
| Architecture and Design; Implementation | - | Disable access to security-sensitive information stored in fuses directly and also reflected from temporary storage locations when in debug mode. | - |

*Source: MITRE CWE Database*

## Recommended Remediations

| Phase | Remediation | Priority |
|-------|-------------|----------|
| Architecture and Design | Implement hardware muxes that automatically disconnect all fuse-sourced registers from the debug interface when debug_mode_i is asserted | Critical |
| Architecture and Design | Apply defense-in-depth by zeroing both individual key signals and aggregated key selection signals during debug mode | High |
| Testing | Verify in post-silicon testing that no fuse-derived value is readable through any debug path, including indirect paths through memory-mapped registers | High |
| Implementation | Maintain a comprehensive register map of all fuse-reflected locations and audit each one for debug-mode protection | Medium |

*These remediations are recommended best practices and are not sourced from MITRE.*
