# CWE-1299 - Missing Protection Mechanism for Alternate Hardware Interface


## Related CVEs

| CVE | Description |
|-----|-------------|
| [CVE-2022-38399](https://www.cve.org/CVERecord?id=CVE-2022-38399) | Missing protection mechanism on serial connection allows for arbitrary OS command execution. |
| [CVE-2020-9285](https://www.cve.org/CVERecord?id=CVE-2020-9285) | Mini-PCI Express slot does not restrict direct memory access. |
| [CVE-2020-8004](https://www.cve.org/CVERecord?id=CVE-2020-8004) | When the internal flash is protected by blocking access on the Data Bus (DBUS), it can still be indirectly accessed through the Instruction Bus (IBUS). |
| [CVE-2017-18293](https://www.cve.org/CVERecord?id=CVE-2017-18293) | When GPIO is protected by blocking access
                        to corresponding GPIO resource registers,
                        protection can be bypassed by writing to the
                        corresponding banked GPIO registers instead. |
| [CVE-2020-15483](https://www.cve.org/CVERecord?id=CVE-2020-15483) | monitor device allows access to physical UART debug port without authentication |

## CWE Explanation

This weakness occurs when a hardware asset (such as a control register or memory region) is accessible through multiple interfaces or address aliases, but access controls are only enforced on the primary path.

In SoC designs, it is common for resources to be accessible via multiple paths for functional reasons. For example, a configuration register might be mapped to two different addresses (aliasing) to support legacy software, or a memory block might be accessible via both a Data Bus (DBUS) and an Instruction Bus (IBUS), or a peripheral might have both a PCIe interface and a debug UART interface.

If the security logic that protects the asset only monitors the primary access path (e.g., the main address or the PCIe interface) and fails to account for the alternate paths, an attacker can bypass the protection mechanism by simply routing their request through the unprotected interface or alias.

## Impact Assessment

| Aspect | Impact |
|--------|--------|
| **Confidentiality** | An attacker can read sensitive data by accessing it through an unprotected debug port or shadow register. |
| **Integrity** | An attacker can modify protected configuration settings by writing to an aliased address that bypasses the write filter. |
| **Availability** | An attacker can reset or disable the device by accessing control registers via an unguarded sideband interface. |

## Attack Scenario

A secure configuration register, `SECURE_CONFIG`, is located at physical address `0x4000_1000`. The hardware access control logic properly restricts write access to this address to only the Trusted Execution Environment (TEE). However, the memory controller design includes a legacy "shadow region" at `0x8000_0000` that mirrors the functionality of the `0x4000_0000` block for backward compatibility. A write to `0x8000_1000` physically updates the same register as a write to `0x4000_1000`. The access control logic was only designed to check transactions targeting the `0x4000_XXXX` range. In the next step, an attacker running unprivileged software writes to `0x8000_1000`. The transaction is not blocked by the firewall because the address does not match the protected range rule, but the hardware effect is identical, allowing the attacker to overwrite the secure configuration.

---

## MITRE Reference Data

### Description

The lack of protections on alternate paths to access control-protected assets (such as unprotected shadow registers and other external facing unguarded interfaces) allows an attacker to bypass existing protections to the asset that are only performed against the primary path.

### Extended Description

An asset inside a chip might have access-control protections through one interface. However, if all paths to the asset are not protected, an attacker might compromise the asset through alternate paths. These alternate paths could be through shadow or mirror registers inside the IP core, or could be paths from other external-facing interfaces to the IP core or SoC.

Consider an SoC with various interfaces such as UART, SMBUS, PCIe, USB, etc. If access control is implemented for SoC internal registers only over the PCIe interface, then an attacker could still modify the SoC internal registers through alternate paths by coming through interfaces such as UART, SMBUS, USB, etc.

Alternatively, attackers might be able to bypass existing protections by exploiting unprotected, shadow registers. Shadow registers and mirror registers typically refer to registers that can be accessed from multiple addresses. Writing to or reading from the aliased/mirrored address has the same effect as writing to the address of the main register. They are typically implemented within an IP core or SoC to temporarily hold certain data. These data will later be updated to the main register, and both registers will be in synch. If the shadow registers are not access-protected, attackers could simply initiate transactions to the shadow registers and compromise system security.

### Demonstrative Examples

#### Example 1

Register SECURE_ME is located at address 0xF00. A mirror of this register called COPY_OF_SECURE_ME is at location 0x800F00. The register SECURE_ME is protected from malicious agents and only allows access to select, while COPY_OF_SECURE_ME is not.

Access control is implemented using an allowlist (as indicated by acl_oh_allowlist). The identity of the initiator of the transaction is indicated by the one hot input, incoming_id. This is checked against the acl_oh_allowlist (which contains a list of initiators that are allowed to access the asset).

```verilog
(bad code)
assign addr_auth = (address == 32'hF00) ? 1: 0;
```

The bugged line of code is repeated in the Bad example above. The weakness arises from the fact that the SECURE_ME register can be modified by writing to the shadow register COPY_OF_SECURE_ME. The address of COPY_OF_SECURE_ME should also be included in the check. That buggy line of code should instead be replaced as shown in the Good Code Snippet below.

```verilog
(good code)
assign addr_auth = (address == 32'hF00 || address == 32'h800F00) ? 1: 0;
```
