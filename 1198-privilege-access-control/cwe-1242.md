# CWE-1242 - Inclusion of Undocumented Features or Chicken Bits


## Related CVEs (from NVD)

| CVE | Description |
|-----|-------------|
| [CVE-2026-24714](https://www.cve.org/CVERecord?id=CVE-2026-24714) | NETGEAR end-of-service products provide undocumented TelnetEnable functionality |
| [CVE-2025-41754](https://www.cve.org/CVERecord?id=CVE-2025-41754) | Undocumented ubr-editfile method in wwwubr.cgi allows remote exploitation |
| [CVE-2025-41756](https://www.cve.org/CVERecord?id=CVE-2025-41756) | Undocumented ubr-editfile method in wwwubr.cgi allows remote exploitation |

## CWE Explanation

This weakness involves the inclusion of hidden, undocumented hardware features or configuration bits (commonly known as "chicken bits") that allow users to bypass standard security controls or modify the device's fundamental behavior.

During the design and validation phases of a semiconductor product, engineers often insert special control bits to disable new or complex features in case they prove buggy (hence "chicken" bits—allowing the designer to "chicken out" of using the new feature). Additionally, undocumented debug modes or "failure analysis" commands may be included to facilitate testing and yield enhancement.

If these mechanisms are not physically disabled or securely locked before the product is shipped to customers, they become potential backdoors. An attacker who discovers these features (often through reverse engineering firmware or fuzzing interfaces) can use them to disable security protections such as encryption, access control firewalls, or secure boot verification, gaining unrestricted access to the device.

## Impact Assessment

| Aspect | Impact |
|--------|--------|
| **Confidentiality** | An attacker can use undocumented features to bypass memory read protections and extract secrets. |
| **Integrity** | An attacker can disable integrity checks (like secure boot) or modify critical system configuration via hidden commands. |
| **Availability** | An attacker can use chicken bits to disable key functionality or place the device into a non-functional test state. |

## Attack Scenario

A device is shipped with a secure boot mechanism designed to verify the digital signature of the firmware before execution. The secure boot logic is stored in an immutable ROM. However, the hardware designers included an undocumented "chicken bit" in a specific memory-mapped control register that, when set, forces the boot ROM to skip the signature verification step. This was intended for use during initial silicon bring-up to speed up testing. The documentation provided to customers does not mention this bit. In the next step, an attacker, having discovered this bit through analysis of leaked engineering data or trial-and-error, exploits a minor software vulnerability to write to this control register. By setting the chicken bit, the attacker disables the secure boot check and subsequently loads malicious, unsigned firmware that grants them permanent control over the device.

---

## MITRE Reference Data

### Description

The device includes chicken bits or undocumented features that can create entry points for unauthorized actors.

### Extended Description

A common design practice is to use undocumented bits on a device that can be used to disable certain functional security features. These bits are commonly referred to as "chicken bits". They can facilitate quick identification and isolation of faulty components, features that negatively affect performance, or features that do not provide the required controllability for debug and test. Another way to achieve this is through implementation of undocumented features.

### Demonstrative Examples

#### Example 1

Consider a device that comes with various security measures, such as secure boot. The secure-boot process performs firmware-integrity verification at boot time, and this code is stored in a separate SPI-flash device. However, this code contains undocumented "special access features" intended to be used only for performing failure analysis and intended to only be unlocked by the device designer.

```other
(bad code)
Attackers dump the code from the device and then perform reverse engineering to analyze the code. The undocumented, special-access features are identified, and attackers can activate them by sending specific commands via UART before secure-boot phase completes. Using these hidden features, attackers can perform reads and writes to memory via the UART interface. At runtime, the attackers can also execute arbitrary code and dump the entire memory contents.
```

Remove all chicken bits and hidden features that are exposed to attackers. Add authorization schemes that rely on cryptographic primitives to access any features that the manufacturer does not want to expose. Clearly document all interfaces.
