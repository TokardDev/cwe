# CWE-1330 - Remanent Data Readable after Memory Erase


## Related CVEs

| CVE | Description |
|-----|-------------|
| [CVE-2019-8575](https://www.cve.org/CVERecord?id=CVE-2019-8575) | Firmware Data Deletion Vulnerability in which a base station factory reset might not delete all user information. The impact of this enables a new owner of a used device that has been &#34;factory-default reset&#34; with a vulnerable firmware version can still retrieve, at least, the previous owner&#39;s wireless network name, and the previous owner&#39;s wireless security (such as WPA2) key. This issue was addressed with improved, data deletion. |

## CWE Explanation

This weakness describes the situation where confidential data stored in memory (such as Flash, EEPROM, SRAM, or DRAM) remains recoverable even after a "clear" or "erase" operation has been performed. This issue typically arises from a disconnect between the logical view of memory (where the file system says the data is gone) and the physical reality of the storage medium.

In many storage technologies, "erasing" data is a costly operation in terms of time and wear. Therefore, systems often optimize by only deleting pointers/metadata (logical delete) or by not ensuring that the physical charge representing the data is fully dissipated (analog remanence). For secure applications, if the physical cells still hold the charge pattern of the original data, an attacker with physical access can read the memory directly, bypassing the logical "empty" status.

The root cause is often performance optimizations that skip secure sanitization steps (like overwriting with zeros/random patterns) or hardware properties like wear-leveling that prevent direct addressing of the physical location where the old data resides.

## Impact Assessment

| Aspect | Impact |
|--------|--------|
| **Confidentiality** | The attacker can read sensitive data that was believed to be destroyed |
| **Integrity** | Low impact (*Primary concern is confidentiality*) |
| **Availability** | Low impact |

## Attack Scenario

An attacker obtains a discarded SSD from a secure server. The server administrator had performed a "quick format" or a standard file deletion command before disposal. However, these commands only removed the file system table entries. The Flash Translation Layer (FTL) inside the SSD still maintains the actual data in physical pages that were marked as "invalid" but not yet erased (garbage collected). The attacker uses a specialized controller or desolders the flash chips to dump the raw memory content, successfully reconstructing the sensitive files that were supposedly deleted.

---

## MITRE Reference Data

### Description

Confidential information stored in memory circuits is readable or recoverable after being cleared or erased.

### Extended Description

Data remanence occurs when stored, memory content is not fully lost after a memory-clear or -erase operation. Confidential memory contents can still be readable through data remanence in the hardware.

Data remanence can occur because of performance optimization or memory organization during 'clear' or 'erase' operations, like a design that allows the memory-organization metadata (e.g., file pointers) to be erased without erasing the actual memory content. To protect against this weakness, memory devices will often support different commands for optimized memory erase and explicit secure erase.

Data remanence can also happen because of the physical properties of memory circuits in use. For example, static, random-access-memory (SRAM) and dynamic, random-access-memory (DRAM) data retention is based on the charge retained in the memory cell, which depends on factors such as power supply, refresh rates, and temperature.

Other than explicit erase commands, self-encrypting, secure-memory devices can also support secure erase through cryptographic erase commands. In such designs, only the decryption keys for encrypted data stored on the device are erased. That is, the stored data are always remnant in the media after a cryptographic erase. However, only the encrypted data can be extracted. Thus, protection against data recovery in such designs relies on the strength of the encryption algorithm.

### Demonstrative Examples

#### Example 1

Consider a device that uses flash memory for non-volatile-data storage. To optimize flash-access performance or reliable-flash lifetime, the device might limit the number of flash writes/erases by maintaining some state in internal SRAM and only committing changes to flash memory periodically.

The device also supports user reset to factory defaults with the expectation that all personal information is erased from the device after this operation. On factory reset, user files are erased using explicit, erase commands supported by the flash device.

In the given, system design, the flash-file system can support performance-optimized erase such that only the file metadata are erased and not the content. If this optimized erase is used for files containing user data during factory-reset flow, then device, flash memory can contain remanent data from these files.

On device-factory reset, the implementation might not erase these copies, since the file organization has changed and the flash file system does not have the metadata to track all previous copies.

A flash-memory region that is used by a flash-file system should be fully erased as part of the factory-reset flow. This should include secure-erase flow for the flash media such as overwriting patterns multiple times followed by erase.

## Remediations from MITRE

| Phase | Strategy | Mitigation | Effectiveness |
|-------|----------|------------|---------------|
| Architecture and Design | — | Support for secure-erase commands that apply multiple cycles of overwriting memory with known patterns and of erasing actual content. Support for cryptographic erase in self-encrypting, memory devices. External, physical tools to erase memory such as ultraviolet-rays-based erase of EEPROM. Physical destruction of media device for repurposed or scrapped devices that are no longer in use. | — |

*Source: MITRE CWE Database*

## Recommended Remediations

| Phase | Remediation | Priority |
|-------|-------------|----------|
| Architecture and Design | Implement hardware-level secure erase that addresses all physical pages including wear-leveled spare blocks, invalid FTL pages, and over-provisioned areas, not just logically mapped addresses. | Critical |
| Architecture and Design | Use self-encrypting storage with a hardware-managed key; on erase, destroy the key (cryptographic erase) to render all remnant data unrecoverable regardless of physical remanence. | Critical |
| Implementation | Ensure factory-reset firmware invokes the storage controller's full secure-erase primitive (e.g., ATA Secure Erase Enhanced, NVMe Crypto Erase) rather than file-system-level deletion. | High |
| Testing | Validate erase completeness by performing raw physical memory dumps post-erase (using chip-off or JTAG techniques) and confirming no recoverable sensitive data patterns remain. | High |

*These remediations are recommended best practices and are not sourced from MITRE.*
